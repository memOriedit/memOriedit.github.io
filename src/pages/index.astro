---
import BaseLayout from '../layouts/BaseLayout.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import {BlogPost, getBlogPosts} from '../lib/microcms';

// Get latest 5 blog posts for the homepage
let latestPosts = [] as BlogPost[];
try {
  const response = await getBlogPosts(5);
  latestPosts = response.contents;
} catch (error) {
  console.error('Failed to fetch blog posts:', error);
}
---

<BaseLayout title="memOriedit">
  <Header />

  <main>
    <!-- Main Visual Section -->
    <section class="hero">
      <div class="container">
        <div class="hero-content">
          <p class="hero-subtitle">消したい記憶はありますか。忘れたい想いはありますか。<br>ここで叶えてあげましょう。あなたを殺してあげましょう。<br><br>記憶を覗き、想いを暴き、苦悩に向き合うカウンセリングアドベンチャー。<br>依頼者の悩みを解決するのか、冷静に職務を全うするのか、決定権は常にあなたにあります。<br>失敗しても気に病むことはありません、正解の道はありません。<br>あなたに責任はありません、あなたに罪はありません。<br><br>あなたの信じる正しさが、誰かを救えますように。</p>
          <div class="hero-cta">
            <a href="https://store.steampowered.com" target="_blank" rel="noopener noreferrer" class="btn btn-primary steam-link">
              Steamで見る
            </a>
          </div>
        </div>
      </div>
    </section>

    <!-- Latest Blog Posts Section -->
    <section class="section latest-posts">
      <div class="container">
        <h2>最新ブログ記事</h2>
        <div class="posts-grid">
          {latestPosts.map((post) => (
            <a href={`/blog/${post.id}`} class="post-card-link">
              <article class="post-card">
                <div class="post-meta">
                  <time datetime={post.date}>{new Date(post.date).toLocaleDateString('ja-JP')}</time>
                </div>
                <h3 class="post-title">
                  {post.title}
                </h3>
              </article>
            </a>
          ))}
        </div>
        <div class="more-posts">
          <a href="/blog" class="btn">もっと見る</a>
        </div>
      </div>
    </section>

    <!-- Game Introduction Section -->
    <section id="game" class="section game-intro">
      <div class="container">
        <div class="warning-notice">
          <h2>🔴🔴🔴【⚠️ 注意事項 ⚠️】🔴🔴🔴</h2>
          <p class="warning-subtitle">➡️ 絶対に読んでください！ ⬅️</p>
          <p class="warning-subtitle">❗重要な内容が含まれています❗</p>
          <p class="warning-subtitle">⛔見落とし厳禁⛔</p>
          
          <div class="warning-content">
            <ul>
              <li>・当店は施術失敗時におけるお客様の安全を保障しません。</li>
              <li>・施術には後遺症(認知症・うつ病・せん妄など)が発生する恐れがあります。</li>
              <li>・上記の事柄に対し、当店が治療費を出すことはありません。</li>
              <li>・以上の点についてご了承いただける方のみ呼び鈴を押してください。</li>
            </ul>
          </div>
        </div>

        <div class="game-content">
          <div class="game-section">
            <h3>【おもいでや】</h3>
            <div class="game-description">
              <p>都会から二駅離れた街、妖しい雰囲気が漂う夜の街。<br>
              路地裏の階段を下った先に、その店はひっそりと。</p>
              
              <p>「消したい記憶を消しましょう」<br>
              「消したい想いを消しましょう」</p>
              
              <p>誰もが一度は考える記憶の消去、その力を使う者は人間なのでしょうか？</p>
            </div>
          </div>

          <div class="game-section">
            <h3>【仕事の内容】</h3>
            <div class="game-description">
              <p>会話と記憶、異なる二つの情報を探るアドベンチャー。<br>
              やってくるお客さんは悩みを抱えていますが、店主がそれに答えるかどうかはあなた次第。<br>
              嘘を見抜き、真意を詳らかにしましょう。大丈夫、難しいことではありません。</p>
              
              <blockquote>
                「相手の望みを聞き出す、あとは適当にやるだけだよ」
              </blockquote>
              
              <p class="final-note">──ほらね？</p>
            </div>
          </div>
        </div>
      </div>
    </section>

    <!-- Staff Introduction Section -->
    <section id="staff" class="section staff-intro">
      <div class="container">
        <h2>スタッフ紹介</h2>
        <div class="staff-grid">
            <a href="https://x.com/ohuro_Kagekiha" target="_blank" rel="noopener noreferrer" class="staff-card-link">
                <div class="staff-card card">
                    <div class="staff-avatar">
                        <img src="/images/staff/kingyo.png" alt="キンギョ" />
                    </div>
                    <div class="staff-info">
                        <h3 class="staff-role">ディレクター・アート・シナリオ</h3>
                        <h4 class="staff-name">
                            キンギョ
                        </h4>
                    </div>
                </div>
            </a>
            <a href="https://x.com/henohenon_8282" target="_blank" rel="noopener noreferrer" class="staff-card-link">
                <div class="staff-card card">
                    <div class="staff-avatar">
                        <img src="/images/staff/henohenon.png" alt="へのへのん" />
                    </div>
                    <div class="staff-info">
                        <h3 class="staff-role">プログラム・進行</h3>
                        <h4 class="staff-name">
                            へのへのん
                        </h4>
                    </div>
                </div>
            </a>
            <a href="https://linktr.ee/kyosuu_maginary" target="_blank" rel="noopener noreferrer" class="staff-card-link">
                <div class="staff-card card">
                    <div class="staff-avatar">
                        <img src="/images/staff/kyosu.jpg" alt="Kyosū!" />
                    </div>
                    <div class="staff-info">
                        <h3 class="staff-role">音楽</h3>
                        <h4 class="staff-name">
                            Kyosū!
                        </h4>
                    </div>
                </div>
            </a>
        </div>
      </div>
    </section>
  </main>

  <Footer />
</BaseLayout>

<style>
  /* Hero Section */
  .hero {
    background: linear-gradient(rgba(0, 0, 0, 0.4), rgba(0, 0, 0, 0.6)), url('/images/key-visual.png');
    background-size: cover;
    background-position: center;
    background-repeat: no-repeat;
    padding: 8rem 0;
    text-align: center;
    position: relative;
    overflow: hidden;
    min-height: 60vh;
  }

  .hero-content {
    position: relative;
    z-index: 2;
  }

  .hero-subtitle {
    font-size: 1.5rem;
    color: var(--text-secondary);
    margin-bottom: 2rem;
    line-height: 1.8;
  }

  .steam-link {
    font-size: 1.2rem;
    padding: 1rem 2rem;
  }

  /* Latest Posts Section */
  .latest-posts {
    background: var(--bg-secondary);
  }

  .posts-grid {
    display: grid;
    gap: 1.5rem;
    margin-bottom: 2rem;
  }

  .post-card-link {
    display: block;
    text-decoration: none;
    color: inherit;
  }

  .post-card {
    background: var(--bg-tertiary);
    padding: 1.5rem;
    border-radius: 8px;
    border: 1px solid var(--border-color);
    transition: transform 0.3s ease, box-shadow 0.3s ease;
  }

  .post-card-link:hover .post-card {
    transform: translateY(-2px);
    box-shadow: 0 4px 12px var(--shadow-dark);
  }

  .post-meta {
    color: var(--text-muted);
    font-size: 0.9rem;
    margin-bottom: 0.5rem;
  }

  .post-title {
    color: var(--text-primary);
    font-size: 1.1rem;
    font-weight: 600;
    margin: 0;
  }

  .post-card-link:hover .post-title {
    color: var(--accent-blue);
  }

  .more-posts {
    text-align: center;
  }

  /* Game Introduction Section */
  .game-intro {
    background: var(--bg-primary);
  }

  .warning-notice {
    background: linear-gradient(135deg, #002a2a 0%, #001a1a 100%);
    border: 2px solid var(--accent-blue);
    border-radius: 8px;
    padding: 2rem;
    margin-bottom: 3rem;
    text-align: center;
  }

  .warning-notice h2 {
    color: #4444ff;
    font-size: 1.5rem;
    margin-bottom: 1rem;
  }

  .warning-subtitle {
    color: #aaaaff;
    font-weight: bold;
    margin-bottom: 0.5rem;
  }

  .warning-content {
    text-align: left;
    margin-top: 1.5rem;
  }

  .warning-content ul {
    list-style: none;
    padding: 0;
  }

  .warning-content li {
    color: var(--text-secondary);
    margin-bottom: 0.5rem;
    padding-left: 1rem;
  }

  .game-content {
    display: grid;
    gap: 3rem;
  }

  .game-section h3 {
    color: var(--accent-blue);
    font-size: 1.8rem;
    margin-bottom: 1.5rem;
  }

  .game-description p {
    margin-bottom: 1.5rem;
    line-height: 1.8;
  }

  blockquote {
    background: var(--bg-secondary);
    border-left: 4px solid var(--accent-blue);
    padding: 1rem 1.5rem;
    margin: 1.5rem 0;
    font-style: italic;
    color: var(--text-primary);
  }

  .final-note {
    text-align: center;
    font-style: italic;
    color: var(--accent-blue);
    font-weight: bold;
  }

  /* Staff Section */
  .staff-intro {
    background: var(--bg-secondary);
  }

  .staff-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
    gap: 2rem;
  }

  .staff-card-link {
    display: block;
    text-decoration: none;
    color: inherit;
  }

  .staff-card {
    text-align: center;
    padding: 2rem;
    transition: transform 0.3s ease, box-shadow 0.3s ease;
  }

  .staff-card-link:hover .staff-card {
    transform: translateY(-2px);
    box-shadow: 0 4px 12px var(--shadow-dark);
  }

  .staff-avatar {
    width: 120px;
    height: 120px;
    margin: 0 auto 1.5rem;
    border-radius: 50%;
    overflow: hidden;
    border: 3px solid var(--border-color);
  }

  .staff-avatar img {
    width: 100%;
    height: 100%;
    object-fit: cover;
  }

  .staff-role {
    color: var(--text-muted);
    font-size: 1rem;
    margin-bottom: 0.5rem;
  }

  .staff-name {
    color: var(--text-primary);
    font-size: 1.3rem;
    font-weight: 600;
    margin: 0;
  }

  .staff-card-link:hover .staff-name {
    color: var(--accent-blue);
  }

  /* Mobile Responsive */
  @media (max-width: 768px) {
    .hero-title {
      font-size: 2.5rem;
    }

    .hero-subtitle {
      font-size: 1.2rem;
    }

    .staff-grid {
      grid-template-columns: 1fr;
    }

    .game-content {
      gap: 2rem;
    }
  }
</style>